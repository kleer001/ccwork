# Dev-only tooling for `bin/ccwork-check`. Not needed to run the GUI or the
# test suite. Kept separate from requirements.txt so production install paths
# and the existing CI step are unchanged.
ruff==0.7.4
mypy==1.13.0
pylint==3.3.1
